#!/bin/python

from distutils.core import setup

setup(name='Scrypt',
      version='0.0.1',
      description='Create and run generate code from AI',
      author='Jerome Rastit',
      packages=['scrypt'],
      )
